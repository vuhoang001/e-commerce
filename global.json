{
  "//": "Makes the mise.toml dotnet pin real. Every SDK on this machine lives in one",
  "//2": "directory, so `dotnet build` silently picks the highest installed version —",
  "//3": "a preview SDK, if one is present — unless a global.json says otherwise.",
  "sdk": {
    "version": "10.0.400",
    "rollForward": "latestFeature"
  },
  "//4": "TUnit runs on Microsoft.Testing.Platform, not VSTest. The .NET 10 SDK refuses",
  "//5": "to run it through the old VSTest bridge, so `dotnet test` has to be told which",
  "//6": "runner to use. Without this, `make test` fails before a single test executes.",
  "test": {
    "runner": "Microsoft.Testing.Platform"
  }
}
